<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Nền trắng mờ -->
    <solid android:color="#4DFFFFFF" /> <!-- 4D = 30% alpha -->

    <!-- Viền trắng -->
    <stroke
        android:width="2dp"
        android:color="@android:color/white" />

    <!-- Bo góc -->
    <corners android:radius="12dp" />

</shape>
